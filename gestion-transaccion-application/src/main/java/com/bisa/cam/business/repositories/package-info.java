/**
 * Los repositorios (repositories) son las interfaces para obtener entidades, así como para crearlas y modificarlas.
 *
 * Mantienen una lista de métodos que se utilizan para comunicarse con las fuentes de datos y devuelven una sola entidad
 * o una lista de entidades. (por ejemplo, UserRepository)
 *
 */
package com.bisa.cam.business.repositories;